// var Datastore = require('nedb');
// var db = {}
// var path = require("path");

// db.feeds = new Datastore({filename: path.join(__dirname, "data",'feeds.db'), autoload: true});
// db.articles = new Datastore({filename: path.join(__dirname, "data",'articles.db'), autoload: true});
// db.widgets = new Datastore({filename: path.join(__dirname, "data",'widgets.db'), autoload: true});


// db.feeds.ensureIndex({ fieldName: 'url', unique: true }, function (err) {
// });


// db.widgets.ensureIndex({ fieldName: 'title', unique: true }, function (err) {
// });

// module.exports = db;
